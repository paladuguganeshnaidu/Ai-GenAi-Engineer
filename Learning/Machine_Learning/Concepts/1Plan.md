learning order:
NumPy → Pandas → Matplotlib
scikit-learn → cuML
PyTorch → torchvision
OpenCV → Albumentations → Ultralytics
transformers → tokenizers → datasets → spaCy
PEFT → bitsandbytes → Accelerate 
FastAPI → Uvicorn → MLflow → Docker